PORT=8788

# Choose how to connect to your MCP server:
MCP_MODE=stdio              # stdio | http
MCP_STDIO_CMD=python        # e.g. "uv"
MCP_STDIO_ARGS=path/to/server.py --stdio

# or HTTP (Streamable HTTP transport)
MCP_HTTP_URL=http://127.0.0.1:8787/mcp

# LLM provider: anthropic | mock
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Optional logging defaults
NODE_ENV=development
