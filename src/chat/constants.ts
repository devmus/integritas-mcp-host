export const DIAGNOSTIC_TOOLS = new Set(["health", "ready"]);

// Only primary, user-facing actions go here.
// (Add "verify_data_with_proof" later.)
export const PRIMARY_TOOLS = new Set(["stamp_data", "verify_data_with_proof"]);
